
# Memory System — Память агента

Скилл для развёртывания и обслуживания системы памяти. Память — это файлы в workspace, которые переживают компакцию контекста. Без памяти агент каждый раз начинает с нуля.

**Принцип:** ВСЁ записывается в файлы. Компакция сожрёт контекст — файлы останутся.

---

## Режим 1: Настройка памяти

**Триггеры:** `настрой память`, `memory setup`

### Что делать:

1. **Проверь структуру workspace.** Создай недостающие папки и файлы:

```
workspace/
├── MEMORY.md          — долгосрочная память
├── HEARTBEAT.md       — правила автообслуживания
├── memory/            — дневные логи
│   └── YYYY-MM-DD.md  — записи за день
└── learning/
    ├── approved-patterns.md  — что сработало
    └── anti-patterns.md      — что НЕ работает
```

2. **MEMORY.md** — если пустой или не существует, заполни базовой структурой из `references/memory-structure.md`

3. **HEARTBEAT.md** — добавь правила автообслуживания из `references/memory-structure.md`

4. **SOUL.md** — добавь в конец правило:
```markdown
## Память
- При каждом важном решении → записывай в `memory/YYYY-MM-DD.md`
- При каждой ошибке → записывай в `learning/anti-patterns.md`
- При успехе → записывай в `learning/approved-patterns.md`
- Раз в 30 минут → делай snapshot в `memory/YYYY-MM-DD.md`
- ВСЁ записывается в файлы. Компакция сожрёт контекст — файлы останутся.
```

5. **Создай сегодняшний лог** `memory/YYYY-MM-DD.md` с базовым шаблоном.

6. **Подтверди:** покажи что создано, что уже было, что добавлено.

---

## Режим 2: Проверка памяти

**Триггеры:** `проверь память`, `memory check`

### Что делать:

#### 1. Проверка дневных логов
- Проверь `memory/YYYY-MM-DD.md` за последние 3 дня
- Существуют ли? Не пустые ли?
- ✅ Есть и заполнены → ОК
- ⚠️ Пустые или отсутствуют → предупреди

#### 2. Проверка MEMORY.md
- Когда последний раз обновлялся?
- Если >7 дней → ⚠️ "MEMORY.md устарел, нужно обновить"
- Проверь что секции заполнены

#### 3. Проверка learning/
- Есть ли записи в `approved-patterns.md`?
- Есть ли записи в `anti-patterns.md`?
- Пустые файлы = агент не учится

#### 4. Консолидация
- Пройди по `memory/*.md` за последние 7 дней
- Собери ключевые решения, инсайты, факты
- Обнови MEMORY.md — добавь новое, убери дубли

#### 5. Чистка
- Найди файлы в `memory/` старше 30 дней
- Предложи архивировать: переместить в `memory/archive/`
- Не удаляй без подтверждения

#### 6. Отчёт
Покажи статистику:

```
🧠 Состояние памяти
━━━━━━━━━━━━━━━━━━
📁 Дневных логов: X файлов
📅 Последняя запись: YYYY-MM-DD
📋 MEMORY.md: актуален / устарел (X дней)
📚 Паттернов: X approved, X anti
🗄️ К архивации: X файлов (>30 дней)

Health Score: X/10
```

**Шкала health score:**
- 10 — всё идеально, логи свежие, MEMORY актуален
- 7-9 — мелкие пробелы (пропущен день, learning пустой)
- 4-6 — проблемы (MEMORY устарел, нет логов за 3+ дня)
- 1-3 — критично (память не работает, всё пустое)

---

## Справочные материалы

- `references/memory-structure.md` — шаблоны файлов
- `references/memory-rules.md` — правила работы с памятью
